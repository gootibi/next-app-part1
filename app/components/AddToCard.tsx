'use client'

import React from 'react'

const AddToCard = () => {
  return (
    <div>
        <button onClick={() => console.log('Click')}>Add to ard</button>
    </div>
  )
}

export default AddToCard